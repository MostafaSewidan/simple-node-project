<!DOCTYPE html>

<html
  lang="ar"
  dir="rtl"
  lang="ar"
  class="light-style layout-navbar-fixed layout-menu-fixed layout-compact"
  dir="rtl"
  data-theme="theme-default"
  data-assets-path="/admin/assets/"
  data-template="vertical-menu-template"
>
  <%- include(modules.component("layouts/head")) %>

  <body>
    <!-- Layout wrapper -->
    <div class="layout-wrapper layout-content-navbar">
      <div class="layout-container">
        <%- include(modules.component("layouts/menu")) %>

        <!-- Layout container -->
        <div class="layout-page">
          <%- include(modules.component("layouts/header")) %>
          <!-- Content wrapper -->
          <div class="content-wrapper">
            <!-- Content -->

            <div class="container-xxl flex-grow-1 container-p-y">
              <!-- @if(!empty($breadcrumb) && count($breadcrumb))
              <x-layout-breadcrumb :breadcrumb="$breadcrumb"/>
          @endif -->
              <!-- </div>-->
              <h1><%= authUser.name %></h1>
              <%- body %>
            </div>
            <!--/ Content -->

            <%- include(modules.component("layouts/footer")) %>
          </div>
          <!--/ Content wrapper -->
        </div>
        <!--/ Layout container -->
      </div>
    </div>

    <!-- Overlay -->
    <div class="layout-overlay layout-menu-toggle"></div>

    <!-- Drag Target Area To SlideIn Menu On Small Screens -->
    <div class="drag-target"></div>

    <%- include(modules.component("layouts/scripts")) %>
  </body>
</html>
